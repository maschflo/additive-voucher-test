### Base assumptions:
- Voucher values are integers

### Restrictions:
- Currency is saved but not used to check again campaign max, campaign max was just an additional feature, can be complicated with multiple currencies.

### Additional Remarks:
- DB table for campaign includes name and slug as metadata for "realism", they are populated but aren't used anywhere.